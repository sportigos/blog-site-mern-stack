//  Begin Date: 2020/05/27  Wed
import axios from 'axios';

import {
    SET_ALL_TAGS,
    SET_TAGS_OF_POST,
    GET_ERRORS
} from "./types";

export const getAllTagsAct = (data) => async dispatch => {
    console.log(data);
}